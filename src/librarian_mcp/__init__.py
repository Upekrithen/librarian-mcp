"""
Librarian MCP — Pre-curated canonical memory + prose/code provenance for any AI coding client.

Five MCP tools:
  librarian_context   — intent-aware canonical memory packet
  prose_provenance    — drift detection between two versions of any document
  record_measurement  — log benchmark measurement to local JSONL
  metrics_summary     — per-vendor / per-model aggregation
  opt_in_share        — toggle anonymous sharing flag
"""

__version__ = "0.2.0"
